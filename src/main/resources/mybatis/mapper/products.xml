<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="products">

	<insert id="insert" parameterType="product">
		insert into products
		(pno, pname, pamount, pcontent, psubtitle, pdifficulty, pprice, pweight, ptime, pmainimg, psubimg1, psubimg2, pmainimgtype, psubimg1type, psubimg2type)
		values
		(pno_seq.nextval, #{pname}, #{pamount}, #{pcontent}, #{psubtitle}, #{pdifficulty}, #{pprice}, #{pweight}, #{ptime}, #{pmainimg}, #{psubimg1}, #{psubimg2}, #{pmainimgtype}, #{psubimg1type}, #{psubimg2type})
	</insert>
</mapper>